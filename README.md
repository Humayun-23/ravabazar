# Client E-commerce Monorepo

* This is a custom e-commerce monorepo.
* `/frontend` is deployed separately to Vercel.
* `/backend` is deployed separately to a VPS using Docker.
* `/docs` contains project planning and handover documentation.
* The Android app will later be created using PWA/TWA from the frontend.
